const {sequelize} = require('../DATABASE/datab');
const {key} = require('PENDIENTE PA MAÑANA ESTA VUELTA');
const jwt = require('jsonwebtoken');


async function validationData(req, res, next) {
    
}
